for f in range(0,len(rekt_boîte)):
			rekt = rekt_boîte[f]
			
			if self.rect.left == rekt.left:
				self.newpos[0] = self.newpos[0] + self.speed[0]
				self.rect = self.rect.move(self.newpos)

			elif self.rect.right == rekt.left:
				self.newpos[0] = self.newpos[0] - self.speed[0]
				self.rect = self.rect.move(self.newpos)